<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- view used for product.product only -->
    <record model="ir.ui.view" id="product_stock_quantities_form_view">
        <field name="name">product.stock_quantities.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view" />
        <field name="arch" type="xml">
            <field name="lst_price" position="before">
                <field name="real_qty_available" string="Available" attrs="{'invisible':[('type', '!=', 'product')]}"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="product_stock_quantities_tree_view">
        <field name="name">product.stock_quantities.tree</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_product_tree_view" />
        <field name="arch" type="xml">
            <field name="virtual_available" position="after">
                <field name="incoming_qty" />
                <field name="outgoing_qty" />
                <field name="real_qty_available" string="Available"/>
            </field>
        </field>
    </record>

    <!-- view used for product.template only -->
    <record model="ir.ui.view" id="template_stock_quantities_form_view">
        <field name="name">template.stock_quantities.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <field name="list_price" position="before">
                <field name="real_qty_available" string="Available" attrs="{'invisible':[('type', '!=', 'product')]}"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="template_stock_quantities_tree_view">
        <field name="name">template.stock_quantities.tree</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view" />
        <field name="arch" type="xml">
            <field name="virtual_available" position="after">
                <field name="incoming_qty" />
                <field name="outgoing_qty" />
                <field name="real_qty_available" string="Available"/>
            </field>
        </field>
    </record>
</odoo>
